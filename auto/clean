#!/bin/sh

# Clean up the live-build working directory, keeping the package
# cache so subsequent builds are fast. Extra arguments (such as
# --purge) are forwarded to lb clean.
#
# "noauto" is REQUIRED: plain "lb clean" would re-execute this
# auto/clean script, recursing forever.
exec lb clean noauto "$@"
