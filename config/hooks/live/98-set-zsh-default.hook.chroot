#!/bin/bash
set -e

apt-get update
apt-get install -y zsh

# Default shell for future users
sed -i 's|^DSHELL=.*|DSHELL=/bin/zsh|' /etc/adduser.conf

# Change shell if the user already exists
if id user >/dev/null 2>&1; then
    usermod -s /bin/zsh user
fi
