---
# Remove the AUREON installer launcher from the target system.
# Runs in the host (live) environment while /target is still
# mounted, right before Calamares unmounts it.
dontChroot: true
timeout: 120
script:
    - "/usr/local/lib/calamares/remove-installer-launcher.sh"